import React from 'react';

const Chat = () => {
  return (
    <div className="fixed bottom-4 right-4 z-50">
      <button className="bg-primary text-white p-3 rounded-full shadow-lg hover:bg-primary-600 transition-colors">
        💬
      </button>
    </div>
  );
};

export default Chat;
